name: streaming_gen_ui
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  llm_json_stream: ^0.4.7
  # llm_tag_parser: ^0.1.0
  llm_tag_parser:
    git:
      url: https://github.com/ComsIndeed/llm-stream-parsers.git
      path: packages/llm/llm_tag_parser_dart
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
