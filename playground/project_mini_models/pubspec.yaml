name: project_mini_models
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  ollama_dart: ^2.2.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  streaming_gen_ui:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
