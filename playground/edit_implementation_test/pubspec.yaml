name: edit_implementation_test
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  streaming_gen_ui:
    path: "../../"
  dartantic_ai: ^3.4.0
  flutter_bloc: ^9.1.1
  http: ^1.6.0
  gpt_markdown: ^1.1.7
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
