import 'package:llm_json_stream/llm_json_stream.dart';

class StreamingWidgetDecoder {
  // ignore: unused_field
  final MapPropertyStream _mapPropertyStream;
  StreamingWidgetDecoder(this._mapPropertyStream);
}
